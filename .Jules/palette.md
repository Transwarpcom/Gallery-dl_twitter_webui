## 2025-05-23 - [Init Palette]
**Learning:** This repo is a Flask app with server-side rendered templates. Infinite scroll is implemented in `user_posts.html` via vanilla JS.
**Action:** When adding UI features like "Back to Top", we need to inject CSS/JS directly into the templates as there's no build system for assets.
